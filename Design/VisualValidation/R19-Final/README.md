# R19 final visual evidence

This directory contains the public, current-release visual evidence only:

- `r19_proxy_candidate.png`: deterministic R19 proxy candidate generated from the current public visual parameters.
- `edge_optics_report.json`: preserved metrics from the approved evaluator run, with private/local absolute paths removed.

The external visual reference and historical experiment rounds are intentionally not included in the public repository.
